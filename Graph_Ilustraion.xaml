<UserControl
    x:Class="app0.Graph_Ilustraion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:oxy="http://oxyplot.org/wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:app0"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="400"
    d:DesignWidth="563"
    mc:Ignorable="d">
    <Grid Margin="10,6,253.667,4.333" Background="Blue">
        <oxy:Plot Title="{Binding VM_Selection}">
            <oxy:Plot.Series>
                <oxy:LineSeries ItemsSource="{Binding VM_GraphList}" Foreground="#FF1F5575"/>
            </oxy:Plot.Series>
        </oxy:Plot>
    <Grid Margin="310,-1,-246,142"/>
        <oxy:Plot Title="{Binding VM_Selection}" Margin="310,-1,-246,205.667">
            <oxy:Plot.Series>
                <oxy:LineSeries ItemsSource="{Binding VM_GraphList}" MarkerType="Circle" MarkerSize="3" MarkerFill="Red" Foreground="#FF1F5575"/>
            </oxy:Plot.Series>
        </oxy:Plot>
        <oxy:Plot Title="{Binding VM_Selection}" Margin="311,197,-247,-0.333">
            <oxy:LineSeries ItemsSource="{Binding VM_GraphList}" Foreground="#FF1F5575"/>
        </oxy:Plot>
    </Grid>

   

</UserControl>