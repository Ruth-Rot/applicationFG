<Window x:Name="OpenFile" x:Class="app0.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:app0"
             xmlns:local="clr-namespace:app0"
             mc:Ignorable="d" 
             d:DesignHeight="800" Width="1000">
    <Window.Background>
        <ImageBrush/>
    </Window.Background>
    <Grid x:Name="grd_panel"  Margin="13,588,38.333,9.667" Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="621*"/>
            <ColumnDefinition Width="334*"/>
        </Grid.ColumnDefinitions>
        <Grid  x:Name="joystick_panel"  HorizontalAlignment="Left" Height="300" Margin="27.667,-335,0,0" VerticalAlignment="Top" Width="300" Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" Grid.Column="1"/>
        <Grid x:Name="measurments_panel" HorizontalAlignment="Left" Height="200" Margin="28.667,-564,0,0" VerticalAlignment="Top" Width="300" Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" Grid.Column="1"/>

    </Grid>


</Window>
  